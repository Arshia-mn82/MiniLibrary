# Bonus UI (Streamlit)

This UI is an optional bonus for the Mini Library.
## Tech
- Streamlit
- Uses the same JSON storage: `data/books.json`

## Run UI
From the project root:

```bash
pip install -r requirements.txt
streamlit run ui/app_streamlit.py
```
